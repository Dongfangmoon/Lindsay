<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration 
	PUBLIC "-//mybatis.org//DTD Config 3.0//EN" 
	"D:/轻量级/mybatis-3-config.dtd" >
<configuration>
<!-- 加载属性文件 -->
<properties resource="database.properties"></properties>
<settings>
<!-- <setting name="logImpl" value="LOG4J" /> -->
<setting name="logImpl" value="STDOUT_LOGGING"/>
<!-- <setting name="outoMappingBehavior" value="NONE"/> -->
</settings>
<!-- 给包中的类注册别名，直接使用类名 -->
<typeAliases>
<package name="cn.easybuy.entity"/>
</typeAliases>
<!--配置环境  复制文档   ctrl+shift+f 自动对齐    -->
<environments default="development">
<environment id="development">
<!-- 配置事务管理 采用JDBC事务管理 -->
<transactionManager type="JDBC" />
<!-- 配置数据源 -->
<dataSource type="POOLED">
<property name="driver" value="${driver}" />
<property name="url" value="${url}" />
<property name="username" value="${username}" />
<property name="password" value="${password}" />
</dataSource>
</environment>
</environments>
<!-- 引用sql映射的xml文件 -->
<mappers>
	<mapper resource="cn/easybuy/dao/user/UserMapper.xml"/>
	<mapper resource="cn/easybuy/dao/product/ProductMapper.xml"/>
 </mappers>
</configuration>
